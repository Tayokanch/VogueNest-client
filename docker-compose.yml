
services:
  vogueshopping_website:
    image: vogueshopping:latest
    container_name: vogueshopping
    ports:
      - "5050:80"

    networks:
      - vogueshopping_network
    restart: always

networks:
  vogueshopping_network:
    driver: bridge
